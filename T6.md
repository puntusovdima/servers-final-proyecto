# Tema 6: Validación Avanzada y Manejo de Errores

## Índice

1. [Validación Avanzada con Zod](#1-validación-avanzada-con-zod)
2. [Middleware de Validación](#2-middleware-de-validación)
3. [Soft Delete (Borrado Lógico)](#3-soft-delete-borrado-lógico)
4. [Manejo de Errores](#4-manejo-de-errores)
5. [Sanitización de Datos](#5-sanitización-de-datos)
6. [Rate Limiting](#6-rate-limiting)
7. [Seguridad con Helmet](#7-seguridad-con-helmet)

---

## 1. Validación Avanzada con Zod

> 📂 **Código de ejemplo**: [`codigo/T6/src/schemas/`](../codigo/T6/src/schemas/)

En T4 y T5 vimos validación básica. Ahora profundizamos en patrones avanzados.

### 1.1 ¿Por qué Zod?

Zod es la librería de validación más popular en 2025 (~65M descargas semanales). Ventajas:

- **Declarativo**: Define validaciones de forma clara y legible
- **Sin dependencias**: Ligero y rápido
- **Composable**: Schemas reutilizables y combinables
- **Transformaciones**: Modifica datos durante validación
- **Mensajes personalizados**: Control total sobre errores

### 1.2 Validación Condicional

```javascript
import { z } from 'zod';

// Schema que valida diferente según el tipo de usuario
const createUserSchema = z.object({
  body: z.object({
    name: z.string().min(2).max(100),
    email: z.string().email(),
    password: z.string().min(8),
    userType: z.enum(['personal', 'business']),
    // Solo requerido si userType es 'business'
    companyName: z.string().optional(),
    taxId: z.string().optional()
  }).refine(
    (data) => {
      if (data.userType === 'business') {
        return data.companyName && data.companyName.length >= 2;
      }
      return true;
    },
    {
      message: 'Las cuentas business requieren nombre de empresa',
      path: ['companyName']
    }
  ).refine(
    (data) => {
      if (data.userType === 'business') {
        return data.taxId && data.taxId.length >= 8;
      }
      return true;
    },
    {
      message: 'Las cuentas business requieren CIF/NIF',
      path: ['taxId']
    }
  )
});
```

### 1.3 Transformaciones

Zod puede transformar datos durante la validación:

```javascript
const productSchema = z.object({
  body: z.object({
    // String a slug
    name: z.string()
      .trim()
      .toLowerCase()
      .transform(s => s.replace(/\s+/g, '-')),
    
    // String a número
    price: z.string()
      .transform(val => parseFloat(val))
      .refine(val => !isNaN(val), 'Debe ser un número')
      .refine(val => val > 0, 'Debe ser positivo'),
    
    // String separado por comas a array
    tags: z.string()
      .transform(val => val.split(',').map(t => t.trim()))
      .pipe(z.array(z.string().min(1))),
    
    // Fecha string a Date
    releaseDate: z.string()
      .transform(val => new Date(val))
      .refine(val => !isNaN(val.getTime()), 'Fecha no válida')
  })
});

// Input: { name: "  Mi Producto  ", price: "29.99", tags: "tech, nuevo, oferta" }
// Output: { name: "mi-producto", price: 29.99, tags: ["tech", "nuevo", "oferta"] }
```

### 1.4 Schemas Reutilizables

```javascript
// src/schemas/common.schema.js
import { z } from 'zod';

// ObjectId de MongoDB
export const objectIdSchema = z
  .string()
  .regex(/^[0-9a-fA-F]{24}$/, 'ID no válido');

// Paginación
export const paginationSchema = z.object({
  page: z.coerce.number().int().positive().default(1),
  limit: z.coerce.number().int().min(1).max(100).default(10),
  sortBy: z.string().optional(),
  order: z.enum(['asc', 'desc']).default('desc')
});

// Rango de fechas
export const dateRangeSchema = z.object({
  startDate: z.coerce.date().optional(),
  endDate: z.coerce.date().optional()
}).refine(
  (data) => {
    if (data.startDate && data.endDate) {
      return data.startDate <= data.endDate;
    }
    return true;
  },
  { message: 'startDate debe ser anterior a endDate' }
);

// Email normalizado
export const emailSchema = z
  .string()
  .email('Email no válido')
  .toLowerCase()
  .trim();

// Password seguro
export const passwordSchema = z
  .string()
  .min(8, 'Mínimo 8 caracteres')
  .regex(/[A-Z]/, 'Debe contener mayúscula')
  .regex(/[a-z]/, 'Debe contener minúscula')
  .regex(/[0-9]/, 'Debe contener número');
```

### 1.5 Composición de Schemas

```javascript
// src/schemas/user.schema.js
import { z } from 'zod';
import { objectIdSchema, emailSchema, passwordSchema, paginationSchema } from './common.schema.js';

// Schema base (campos comunes)
const userBaseSchema = z.object({
  name: z.string().min(2).max(100).trim(),
  email: emailSchema,
  age: z.number().int().min(0).max(120).optional(),
  avatar: z.string().url().optional().nullable()
});

// Crear usuario (campos requeridos + password)
export const createUserSchema = z.object({
  body: userBaseSchema.extend({
    password: passwordSchema
  })
});

// Actualizar usuario (todos opcionales)
export const updateUserSchema = z.object({
  params: z.object({ id: objectIdSchema }),
  body: userBaseSchema.partial().refine(
    (data) => Object.keys(data).length > 0,
    { message: 'Debe enviar al menos un campo' }
  )
});

// Listar usuarios (query params)
export const listUsersSchema = z.object({
  query: paginationSchema.extend({
    search: z.string().max(100).optional(),
    isActive: z.enum(['true', 'false']).transform(v => v === 'true').optional()
  })
});
```

### 1.6 Validación de Archivos

```javascript
// src/schemas/file.schema.js
import { z } from 'zod';

// Validar archivo subido (después de Multer)
export const imageFileSchema = z.object({
  fieldname: z.string(),
  originalname: z.string(),
  mimetype: z.enum(
    ['image/jpeg', 'image/png', 'image/gif', 'image/webp'],
    { errorMap: () => ({ message: 'Solo imágenes (jpeg, png, gif, webp)' }) }
  ),
  size: z.number().max(5 * 1024 * 1024, 'Máximo 5MB')
});

export const audioFileSchema = z.object({
  fieldname: z.string(),
  originalname: z.string(),
  mimetype: z.enum(
    ['audio/mpeg', 'audio/wav', 'audio/ogg'],
    { errorMap: () => ({ message: 'Solo audio (mp3, wav, ogg)' }) }
  ),
  size: z.number().max(50 * 1024 * 1024, 'Máximo 50MB')
});

// Middleware para validar archivo
export const validateFile = (schema) => (req, res, next) => {
  if (!req.file) {
    return res.status(400).json({ error: true, message: 'Archivo requerido' });
  }
  
  const result = schema.safeParse(req.file);
  
  if (!result.success) {
    return res.status(400).json({
      error: true,
      message: result.error.errors[0].message
    });
  }
  
  next();
};
```

---

## 2. Middleware de Validación

> 📂 **Código de ejemplo**: [`codigo/T6/src/middleware/validate.middleware.js`](../codigo/T6/src/middleware/validate.middleware.js)

### 2.1 Middleware Genérico

```javascript
// src/middleware/validate.middleware.js
import mongoose from 'mongoose';

/**
 * Middleware de validación con Zod
 * Valida body, query y params
 */
export const validate = (schema) => (req, res, next) => {
  try {
    schema.parse({
      body: req.body,
      query: req.query,
      params: req.params
    });
    next();
  } catch (error) {
    const errors = error.errors.map(e => ({
      field: e.path.join('.'),
      message: e.message
    }));
    
    res.status(400).json({
      error: true,
      message: 'Error de validación',
      code: 'VALIDATION_ERROR',
      details: errors
    });
  }
};

/**
 * Validar solo body (más común)
 */
export const validateBody = (schema) => (req, res, next) => {
  try {
    req.body = schema.parse(req.body);
    next();
  } catch (error) {
    const errors = error.errors.map(e => ({
      field: e.path.join('.'),
      message: e.message
    }));
    
    res.status(400).json({
      error: true,
      message: 'Error de validación',
      code: 'VALIDATION_ERROR',
      details: errors
    });
  }
};

/**
 * Validar ObjectId de MongoDB
 */
export const validateObjectId = (paramName = 'id') => (req, res, next) => {
  const id = req.params[paramName];
  
  if (!mongoose.Types.ObjectId.isValid(id)) {
    return res.status(400).json({
      error: true,
      message: `'${paramName}' no es un ID válido`,
      code: 'INVALID_ID'
    });
  }
  
  next();
};
```

### 2.2 Uso en Rutas

```javascript
// src/routes/users.routes.js
import { Router } from 'express';
import * as controller from '../controllers/users.controller.js';
import { validate, validateObjectId } from '../middleware/validate.middleware.js';
import { 
  createUserSchema, 
  updateUserSchema, 
  listUsersSchema 
} from '../schemas/user.schema.js';

const router = Router();

router.get('/', validate(listUsersSchema), controller.getUsers);
router.get('/:id', validateObjectId(), controller.getUser);
router.post('/', validate(createUserSchema), controller.createUser);
router.put('/:id', validate(updateUserSchema), controller.updateUser);
router.delete('/:id', validateObjectId(), controller.deleteUser);

export default router;
```

---

## 3. Soft Delete (Borrado Lógico)

> 📂 **Código de ejemplo**: [`codigo/T6/src/plugins/softDelete.plugin.js`](../codigo/T6/src/plugins/softDelete.plugin.js) y [`codigo/T6/src/models/track.model.js`](../codigo/T6/src/models/track.model.js)

El borrado lógico marca registros como eliminados sin borrarlos físicamente, permitiendo recuperación y auditoría.

### 3.1 ¿Por qué Soft Delete?

| Hard Delete | Soft Delete |
|-------------|-------------|
| Datos perdidos permanentemente | Datos recuperables |
| No hay historial | Auditoría completa |
| Simple | Requiere filtros adicionales |
| Puede romper relaciones | Mantiene integridad referencial |

### 3.2 Plugin Reutilizable de Mongoose

```javascript
// src/plugins/softDelete.plugin.js

/**
 * Plugin de Mongoose para soft delete
 * Añade campos: deleted, deletedAt, deletedBy
 * Añade métodos: softDelete(), restore()
 * Filtra automáticamente documentos eliminados
 */
export const softDeletePlugin = (schema) => {
  // Añadir campos al schema
  schema.add({
    deleted: { type: Boolean, default: false, index: true },
    deletedAt: { type: Date, default: null },
    deletedBy: { type: String, default: null }
  });
  
  // Middleware: excluir eliminados en consultas
  const excludeDeleted = function() {
    if (!this.getOptions().withDeleted) {
      this.where({ deleted: { $ne: true } });
    }
  };
  
  schema.pre('find', excludeDeleted);
  schema.pre('findOne', excludeDeleted);
  schema.pre('findOneAndUpdate', excludeDeleted);
  schema.pre('countDocuments', excludeDeleted);
  
  // Método de instancia: soft delete
  schema.methods.softDelete = async function(deletedBy = null) {
    this.deleted = true;
    this.deletedAt = new Date();
    this.deletedBy = deletedBy;
    return this.save();
  };
  
  // Método de instancia: restaurar
  schema.methods.restore = async function() {
    this.deleted = false;
    this.deletedAt = null;
    this.deletedBy = null;
    return this.save();
  };
  
  // Método estático: soft delete por ID
  schema.statics.softDeleteById = async function(id, deletedBy = null) {
    return this.findByIdAndUpdate(
      id,
      {
        deleted: true,
        deletedAt: new Date(),
        deletedBy
      },
      { new: true }
    ).setOptions({ withDeleted: true });
  };
  
  // Método estático: restaurar por ID
  schema.statics.restoreById = async function(id) {
    return this.findByIdAndUpdate(
      id,
      {
        deleted: false,
        deletedAt: null,
        deletedBy: null
      },
      { new: true }
    ).setOptions({ withDeleted: true });
  };
  
  // Método estático: buscar incluyendo eliminados
  schema.statics.findWithDeleted = function(filter = {}) {
    return this.find(filter).setOptions({ withDeleted: true });
  };
  
  // Método estático: buscar solo eliminados
  schema.statics.findDeleted = function(filter = {}) {
    return this.find({ ...filter, deleted: true }).setOptions({ withDeleted: true });
  };
  
  // Método estático: eliminar permanentemente
  schema.statics.hardDelete = function(id) {
    return this.findByIdAndDelete(id).setOptions({ withDeleted: true });
  };
};
```

### 3.3 Aplicar el Plugin

```javascript
// src/models/track.model.js
import mongoose from 'mongoose';
import { softDeletePlugin } from '../plugins/softDelete.plugin.js';

const trackSchema = new mongoose.Schema({
  title: { type: String, required: true },
  artist: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true },
  duration: { type: Number, required: true },
  genres: [String],
  plays: { type: Number, default: 0 }
}, {
  timestamps: true,
  versionKey: false
});

// Aplicar plugin de soft delete
trackSchema.plugin(softDeletePlugin);

const Track = mongoose.model('Track', trackSchema);
export default Track;
```

### 3.4 Uso en Controladores

```javascript
// src/controllers/tracks.controller.js
import Track from '../models/track.model.js';

// DELETE /api/tracks/:id - Soft delete
export const deleteTrack = async (req, res) => {
  const track = await Track.softDeleteById(req.params.id, req.user?._id?.toString());
  
  if (!track) {
    return res.status(404).json({ error: true, message: 'Track no encontrado' });
  }
  
  res.json({ 
    message: 'Track eliminado',
    data: { id: track._id, deletedAt: track.deletedAt }
  });
};

// PATCH /api/tracks/:id/restore - Restaurar
export const restoreTrack = async (req, res) => {
  const track = await Track.restoreById(req.params.id);
  
  if (!track) {
    return res.status(404).json({ error: true, message: 'Track no encontrado' });
  }
  
  res.json({ message: 'Track restaurado', data: track });
};

// GET /api/tracks/deleted - Listar eliminados
export const getDeletedTracks = async (req, res) => {
  const tracks = await Track.findDeleted()
    .populate('artist', 'name email')
    .sort({ deletedAt: -1 });
  
  res.json({ data: tracks });
};

// DELETE /api/tracks/:id/permanent - Eliminar permanentemente
export const hardDeleteTrack = async (req, res) => {
  const track = await Track.hardDelete(req.params.id);
  
  if (!track) {
    return res.status(404).json({ error: true, message: 'Track no encontrado' });
  }
  
  res.status(204).send();
};
```

---

## 4. Manejo de Errores

> 📂 **Código de ejemplo**: [`codigo/T6/src/utils/AppError.js`](../codigo/T6/src/utils/AppError.js) y [`codigo/T6/src/middleware/error.middleware.js`](../codigo/T6/src/middleware/error.middleware.js)

### 4.1 Clase de Error Personalizada

```javascript
// src/utils/AppError.js

export class AppError extends Error {
  constructor(message, statusCode = 500, code = null) {
    super(message);
    this.statusCode = statusCode;
    this.code = code;
    this.isOperational = true;
    
    Error.captureStackTrace(this, this.constructor);
  }
  
  // Factory methods para errores comunes
  static badRequest(message = 'Solicitud inválida', code = 'BAD_REQUEST') {
    return new AppError(message, 400, code);
  }
  
  static unauthorized(message = 'No autorizado', code = 'UNAUTHORIZED') {
    return new AppError(message, 401, code);
  }
  
  static forbidden(message = 'Acceso prohibido', code = 'FORBIDDEN') {
    return new AppError(message, 403, code);
  }
  
  static notFound(resource = 'Recurso', code = 'NOT_FOUND') {
    return new AppError(`${resource} no encontrado`, 404, code);
  }
  
  static conflict(message = 'Conflicto con recurso existente', code = 'CONFLICT') {
    return new AppError(message, 409, code);
  }
  
  static validation(message = 'Error de validación', details = []) {
    const error = new AppError(message, 400, 'VALIDATION_ERROR');
    error.details = details;
    return error;
  }
  
  static tooManyRequests(message = 'Demasiadas peticiones', code = 'RATE_LIMIT') {
    return new AppError(message, 429, code);
  }
  
  static internal(message = 'Error interno del servidor', code = 'INTERNAL_ERROR') {
    return new AppError(message, 500, code);
  }
}
```

### 4.2 Middleware Global de Errores

```javascript
// src/middleware/error.middleware.js
import mongoose from 'mongoose';
import { AppError } from '../utils/AppError.js';

/**
 * Middleware para rutas no encontradas
 */
export const notFound = (req, res, next) => {
  next(AppError.notFound(`Ruta ${req.method} ${req.originalUrl}`));
};

/**
 * Middleware global de errores
 */
export const errorHandler = (err, req, res, next) => {
  console.error('❌ Error:', err.message);
  
  // Error operacional (esperado)
  if (err.isOperational) {
    return res.status(err.statusCode).json({
      error: true,
      message: err.message,
      code: err.code,
      ...(err.details && { details: err.details })
    });
  }
  
  // Error de validación de Mongoose
  if (err instanceof mongoose.Error.ValidationError) {
    const details = Object.values(err.errors).map(e => ({
      field: e.path,
      message: e.message
    }));
    return res.status(400).json({
      error: true,
      message: 'Error de validación',
      code: 'VALIDATION_ERROR',
      details
    });
  }
  
  // Error de Cast (ID inválido)
  if (err instanceof mongoose.Error.CastError) {
    return res.status(400).json({
      error: true,
      message: `Valor inválido para '${err.path}'`,
      code: 'CAST_ERROR'
    });
  }
  
  // Error de duplicado (unique constraint)
  if (err.code === 11000) {
    const field = Object.keys(err.keyValue || {})[0];
    return res.status(409).json({
      error: true,
      message: `Ya existe un registro con ese '${field}'`,
      code: 'DUPLICATE_KEY'
    });
  }
  
  // Error de Zod
  if (err.name === 'ZodError') {
    const details = err.errors.map(e => ({
      field: e.path.join('.'),
      message: e.message
    }));
    return res.status(400).json({
      error: true,
      message: 'Error de validación',
      code: 'VALIDATION_ERROR',
      details
    });
  }
  
  // Error de Multer
  if (err.code === 'LIMIT_FILE_SIZE') {
    return res.status(400).json({
      error: true,
      message: 'Archivo muy grande',
      code: 'FILE_TOO_LARGE'
    });
  }
  
  if (err.code === 'LIMIT_FILE_COUNT') {
    return res.status(400).json({
      error: true,
      message: 'Demasiados archivos',
      code: 'TOO_MANY_FILES'
    });
  }
  
  // Error no controlado
  const isProduction = process.env.NODE_ENV === 'production';
  res.status(500).json({
    error: true,
    message: isProduction ? 'Error interno del servidor' : err.message,
    code: 'INTERNAL_ERROR',
    ...(!isProduction && { stack: err.stack })
  });
};
```

### 4.3 Uso de Errores en Controladores

```javascript
// src/controllers/users.controller.js
import User from '../models/user.model.js';
import { AppError } from '../utils/AppError.js';

export const getUser = async (req, res) => {
  const user = await User.findById(req.params.id);
  
  if (!user) {
    throw AppError.notFound('Usuario');
  }
  
  res.json({ data: user });
};

export const createUser = async (req, res) => {
  // Verificar si ya existe
  const existing = await User.findOne({ email: req.body.email });
  if (existing) {
    throw AppError.conflict('El email ya está registrado');
  }
  
  const user = await User.create(req.body);
  res.status(201).json({ data: user });
};
```

---

## 5. Sanitización de Datos

> 📂 **Código de ejemplo**: [`codigo/T6/src/middleware/sanitize.middleware.js`](../codigo/T6/src/middleware/sanitize.middleware.js)

### 5.1 Prevenir Inyección NoSQL

```javascript
// src/middleware/sanitize.middleware.js

/**
 * Elimina operadores MongoDB peligrosos de req.body
 * Previene ataques de inyección NoSQL como:
 * { "email": { "$gt": "" }, "password": { "$gt": "" } }
 */
export const sanitizeBody = (req, res, next) => {
  if (req.body) {
    const sanitize = (obj) => {
      for (const key in obj) {
        // Eliminar claves que empiezan con $ (operadores MongoDB)
        if (key.startsWith('$')) {
          delete obj[key];
        } else if (typeof obj[key] === 'object' && obj[key] !== null) {
          sanitize(obj[key]);
        }
      }
    };
    sanitize(req.body);
  }
  next();
};

/**
 * Limita el tamaño de strings para prevenir DoS
 */
export const limitStringLength = (maxLength = 10000) => (req, res, next) => {
  const truncate = (obj) => {
    for (const key in obj) {
      if (typeof obj[key] === 'string' && obj[key].length > maxLength) {
        obj[key] = obj[key].substring(0, maxLength);
      } else if (typeof obj[key] === 'object' && obj[key] !== null) {
        truncate(obj[key]);
      }
    }
  };
  
  if (req.body) truncate(req.body);
  next();
};

/**
 * Elimina campos no permitidos del body
 */
export const allowedFields = (...fields) => (req, res, next) => {
  if (req.body) {
    const filtered = {};
    for (const field of fields) {
      if (req.body[field] !== undefined) {
        filtered[field] = req.body[field];
      }
    }
    req.body = filtered;
  }
  next();
};
```

### 5.2 Aplicar Sanitización

```javascript
// src/app.js
import express from 'express';
import { sanitizeBody, limitStringLength } from './middleware/sanitize.middleware.js';

const app = express();

app.use(express.json({ limit: '10kb' }));
app.use(sanitizeBody);
app.use(limitStringLength(5000));

// ... resto de la configuración
```

### 5.3 Alternativa: express-mongo-sanitize

Para producción, considera usar el paquete probado `express-mongo-sanitize`:

```bash
npm install express-mongo-sanitize
```

```javascript
import mongoSanitize from 'express-mongo-sanitize';

// Elimina claves que contienen $ o .
app.use(mongoSanitize());

// O reemplazar caracteres prohibidos
app.use(mongoSanitize({
  replaceWith: '_',
  onSanitize: ({ key, req }) => {
    console.warn(`Intento de inyección NoSQL detectado: ${key}`);
  }
}));
```

---

## 6. Rate Limiting

> 📂 **Código de ejemplo**: [`codigo/T6/src/middleware/rateLimit.middleware.js`](../codigo/T6/src/middleware/rateLimit.middleware.js)

### 6.1 Implementación Simple

```javascript
// src/middleware/rateLimit.middleware.js

// Almacén en memoria (usar Redis en producción)
const requestCounts = new Map();

/**
 * Limita peticiones por IP
 * @param {number} maxRequests - Máximo de peticiones permitidas
 * @param {number} windowMs - Ventana de tiempo en milisegundos
 */
export const rateLimit = (maxRequests = 100, windowMs = 60 * 1000) => {
  return (req, res, next) => {
    const ip = req.ip || req.connection.remoteAddress;
    const now = Date.now();
    
    let record = requestCounts.get(ip);
    
    if (!record || now - record.startTime > windowMs) {
      // Nueva ventana de tiempo
      record = { count: 1, startTime: now };
      requestCounts.set(ip, record);
    } else {
      record.count++;
    }
    
    // Headers informativos
    res.setHeader('X-RateLimit-Limit', maxRequests);
    res.setHeader('X-RateLimit-Remaining', Math.max(0, maxRequests - record.count));
    res.setHeader('X-RateLimit-Reset', new Date(record.startTime + windowMs).toISOString());
    
    if (record.count > maxRequests) {
      return res.status(429).json({
        error: true,
        message: 'Demasiadas peticiones. Intenta más tarde.',
        code: 'RATE_LIMIT',
        retryAfter: Math.ceil((record.startTime + windowMs - now) / 1000)
      });
    }
    
    next();
  };
};

// Limpieza periódica del Map
setInterval(() => {
  const now = Date.now();
  for (const [ip, record] of requestCounts.entries()) {
    if (now - record.startTime > 60 * 1000) {
      requestCounts.delete(ip);
    }
  }
}, 60 * 1000);
```

### 6.2 Uso en Rutas

```javascript
// src/routes/tracks.routes.js
import { Router } from 'express';
import { rateLimit } from '../middleware/rateLimit.middleware.js';
import * as controller from '../controllers/tracks.controller.js';

const router = Router();

// Rate limit para creación: máx 10 por minuto
router.post('/', 
  rateLimit(10, 60 * 1000),
  controller.createTrack
);

// Rate limit para plays: máx 60 por minuto (1 por segundo)
router.post('/:id/play',
  rateLimit(60, 60 * 1000),
  controller.playTrack
);

export default router;
```

### 6.3 Alternativa: express-rate-limit

Para producción, usa `express-rate-limit` con almacenamiento Redis:

```bash
npm install express-rate-limit
```

```javascript
import rateLimit from 'express-rate-limit';

// Configuración global
const limiter = rateLimit({
  windowMs: 15 * 60 * 1000, // 15 minutos
  max: 100, // 100 peticiones por ventana
  message: {
    error: true,
    message: 'Demasiadas peticiones, intenta en 15 minutos',
    code: 'RATE_LIMIT'
  },
  standardHeaders: true, // Headers RateLimit-*
  legacyHeaders: false   // Desactiva X-RateLimit-*
});

app.use(limiter);

// Límite específico para login (más estricto)
const loginLimiter = rateLimit({
  windowMs: 60 * 60 * 1000, // 1 hora
  max: 5, // 5 intentos
  message: {
    error: true,
    message: 'Demasiados intentos de login',
    code: 'LOGIN_RATE_LIMIT'
  }
});

app.post('/api/auth/login', loginLimiter, authController.login);
```

---

## 7. Seguridad con Helmet

Helmet configura headers HTTP para proteger tu aplicación de vulnerabilidades comunes.

### 7.1 Instalación y Uso Básico

```bash
npm install helmet
```

```javascript
import helmet from 'helmet';

const app = express();

// Configuración por defecto (recomendada)
app.use(helmet());
```

### 7.2 ¿Qué protege Helmet?

| Header | Protección |
|--------|------------|
| `Content-Security-Policy` | Previene XSS e inyección de datos |
| `X-Content-Type-Options` | Evita MIME sniffing |
| `X-Frame-Options` | Previene clickjacking |
| `Strict-Transport-Security` | Fuerza HTTPS |
| `X-XSS-Protection` | Filtro XSS del navegador |
| `X-DNS-Prefetch-Control` | Controla prefetch DNS |

### 7.3 Configuración Personalizada

```javascript
app.use(helmet({
  contentSecurityPolicy: {
    directives: {
      defaultSrc: ["'self'"],
      styleSrc: ["'self'", "'unsafe-inline'"],
      imgSrc: ["'self'", "data:", "https:"],
      scriptSrc: ["'self'"]
    }
  },
  crossOriginResourcePolicy: { policy: "cross-origin" } // Para servir imágenes a otros dominios
}));
```

### 7.4 Configuración Completa de Seguridad

```javascript
// src/app.js
import express from 'express';
import helmet from 'helmet';
import cors from 'cors';
import mongoSanitize from 'express-mongo-sanitize';
import rateLimit from 'express-rate-limit';

const app = express();

// 1. Seguridad HTTP
app.use(helmet());

// 2. CORS
app.use(cors({
  origin: process.env.CORS_ORIGIN || 'http://localhost:5173',
  credentials: true
}));

// 3. Rate limiting
app.use(rateLimit({
  windowMs: 15 * 60 * 1000,
  max: 100
}));

// 4. Body parser con límite
app.use(express.json({ limit: '10kb' }));

// 5. Sanitización NoSQL
app.use(mongoSanitize());

// ... rutas
```

---

## 8. Ejercicios Prácticos

### Ejercicio 1: Validación de Productos

Crea schemas de validación para un e-commerce:

```javascript
// Producto con variantes
{
  name: "Camiseta",
  price: 29.99,
  category: "ropa",
  variants: [
    { size: "S", color: "rojo", stock: 10 },
    { size: "M", color: "azul", stock: 5 }
  ],
  discount: { percentage: 10, validUntil: "2025-12-31" }
}
```

Requisitos:
- `price` debe ser positivo
- `category` debe ser uno de: ropa, electrónica, hogar
- Cada variante debe tener size, color y stock >= 0
- Si hay `discount`, validar que `validUntil` sea fecha futura

### Ejercicio 2: Sistema de Comentarios con Soft Delete

Implementa un modelo `Comment` con:
- content (string, max 500)
- author (ref User)
- post (ref Post)
- likes (number)

Requisitos:
- Aplicar soft delete plugin
- Crear endpoints: crear, listar, eliminar, restaurar
- Solo el autor puede eliminar sus comentarios
- Admin puede ver y restaurar eliminados

### Ejercicio 3: Rate Limiting Avanzado

Extiende el rate limiter para:
- Diferentes límites por ruta
- Diferentes límites por rol (user: 100/min, admin: 1000/min)
- Almacenamiento en un Map por ruta+IP

---

## Ejercicio del Curso

Para poner en práctica los conceptos de este tema, realiza el siguiente ejercicio:

### 🗑️ Sistema de Notas con Soft Delete

Crea una API de notas con borrado lógico, recuperación y sistema robusto de errores.

**Ubicación:** `ejercicios/T6/`

**Modelo Note:**
- title, content, color (enum), pinned (boolean)
- Campos de soft delete: deleted, deletedAt

**Endpoints:**
| Método | Ruta | Descripción |
|--------|------|-------------|
| GET | /api/notes | Listar (excluye eliminadas) |
| GET | /api/notes/trash | Listar papelera |
| GET | /api/notes/:id | Obtener una |
| POST | /api/notes | Crear |
| PUT | /api/notes/:id | Actualizar |
| DELETE | /api/notes/:id | Soft delete |
| POST | /api/notes/:id/restore | Restaurar |
| DELETE | /api/notes/:id/permanent | Hard delete |
| DELETE | /api/notes/trash/empty | Vaciar papelera |

**Conceptos a aplicar:**
- Soft delete con Mongoose
- Clase AppError con factory methods
- Error handler centralizado
- Validación con Zod

```bash
cd ejercicios/T6
npm install
npm run dev
```

Consulta el README del ejercicio para más detalles.

---

## Resumen

| Concepto | Descripción |
|----------|-------------|
| Zod | Validación type-safe con transformaciones |
| refine() | Validaciones condicionales personalizadas |
| transform() | Modificar datos durante validación |
| Soft Delete | Borrado lógico con recuperación |
| Plugin Mongoose | Funcionalidad reutilizable entre modelos |
| AppError | Clase de error con factory methods |
| Sanitización | Prevenir inyección NoSQL |
| Rate Limiting | Limitar peticiones por IP/tiempo |

---

## Recursos Adicionales

- [Zod Documentation](https://zod.dev/)
- [Mongoose Plugins](https://mongoosejs.com/docs/plugins.html)
- [Helmet.js](https://helmetjs.github.io/)
- [express-rate-limit](https://github.com/express-rate-limit/express-rate-limit)
- [express-mongo-sanitize](https://github.com/fiznool/express-mongo-sanitize)
- [OWASP NoSQL Injection](https://owasp.org/www-community/attacks/NoSQL_Injection)
- [Express Error Handling](https://expressjs.com/en/guide/error-handling.html)

### Próximo tema

En el **Tema 7 - Autenticación y Autorización** veremos:
- Autenticación con JWT (access + refresh tokens)
- Hashing de contraseñas con bcrypt
- Middleware de autenticación
- Sistema de roles y permisos
- Protección de rutas
